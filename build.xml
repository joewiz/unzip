<?xml version="1.0" encoding="UTF-8"?>
<project default="xar" name="unzip">
    <property name="build.dir" value="build"/>
    <target name="xar">
        <mkdir dir="${build.dir}"/>
        <zip basedir="." destfile="${build.dir}/unzip-0.1.3.xar" excludes="${build.dir}/"/>
    </target>
</project>